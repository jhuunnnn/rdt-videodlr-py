X initalize py in vs code
X write and read txt.file
X count how many lines ina txt.file
X execute a fnc every lines
X make fnc to dl a video from video
    X output the video in specfic path
    Z name the video through its content
X check first if the link.txt is not empty
>>> redo dl py fnc fixed by instaliing ffmpeg to the system
X deploy fnc RedditVL to the while loop
X complie all video into one folder




add todoList
    ffmpeg as py module only to easier setup
    update readMe
    filter videos by folders category
    Make the program a application has better ui